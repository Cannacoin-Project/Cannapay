var express = require('express');
var router = express.Router();

/*    
// SHOW LIST OF USERS @ CANNAPAY
router.get('/',  function(req, res) {
    var db = req.db;
    db.collection('users').find().toArray(function (err, items) {
        res.json(items);
    });
});
*/

module.exports = router;